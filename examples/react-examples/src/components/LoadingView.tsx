export function LoadingView() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <div style={{ fontSize: "1.5rem", color: "#646cff" }}>Loading...</div>
    </div>
  );
}
